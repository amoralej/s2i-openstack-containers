contentOrigin:
  repofiles:
    - ./rpms.repo
context:
  bare: true

#
# To update rpms.lock.yaml:
#    rpm-lockfile-prototype rpms.in.yaml
#

arches:
  - x86_64
  - aarch64

packages:
  - ca-certificates
  - curl
  - glibc-langpack-en
  - procps-ng
  - python3
  - python3-pip
  - sudo
  - util-linux-user
  - which
